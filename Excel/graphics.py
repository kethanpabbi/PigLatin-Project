from tkinter import W


class Worksheet:
    def __init__(self, name, number):
        self.name = name
        self.number = number
    def read_cell(label):
        self.label = label
        pass
    def write_cell(label, newval):
        newval = 
        return label
    def append(self, newvals):
        col[]
        pass
    def show(self):
        print('%8s %8s %8s %8s' %('A', 'B', 'C', 'D'))
        return '{} {}'.format(self.name, self.number)


